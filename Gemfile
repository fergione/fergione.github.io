source "https://rubygems.org"

# Specify Jekyll and Bundler
gem "jekyll"
gem "bundler"

# Uncomment the following if you need additional plugins:
# gem "jekyll-feed"      # Adds RSS feed support
# gem "jekyll-seo-tag"   # Adds SEO metadata
# gem "jekyll-sitemap"   # Generates a sitemap.xml
# gem "webrick"          # Required for local serving in some Ruby versions

# Run `bundle install` to install dependencies